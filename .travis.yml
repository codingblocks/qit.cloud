before_install: cd website
language: node_js
node_js:
  - "10"
dist: trusty
script:
  - npm run concurrentlyScripts
install:
  - npm run ci
  - npm run testBuild
cache:
  directories:
    - ~/.npm
    - ~/.cache
branches:
  only:
    - master
env:
  CI=true