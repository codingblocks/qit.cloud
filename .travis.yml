language: node_js
node_js:
  - "10"
script:
  - npm run concurrentlyTests
install:
  - npm run concurrentlyInstalls
cache:
  directories:
    - ~/.npm
    - ~/.cache
branches:
  only:
    - master
    - Cypress
    - feature/travisFix
