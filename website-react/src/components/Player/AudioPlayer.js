import styled from 'styled-components'

export default styled.audio`
  width: calc(99%);
  max-width: 796px;
  position: fixed;
  bottom: 2px;

  &::-webkit-media-controls-panel {
    background: #4F8F88;
  }
`
